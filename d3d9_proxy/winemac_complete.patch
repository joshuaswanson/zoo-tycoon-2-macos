diff --git a/dlls/winemac.drv/cocoa_app.m b/dlls/winemac.drv/cocoa_app.m
index b433838..74f05a4 100644
--- a/dlls/winemac.drv/cocoa_app.m
+++ b/dlls/winemac.drv/cocoa_app.m
@@ -19,6 +19,7 @@
  */
 
 #import <Carbon/Carbon.h>
+#import <objc/runtime.h>
 
 #import "cocoa_app.h"
 #import "cocoa_cursorclipping.h"
@@ -311,6 +312,70 @@ - (void) changeEditMenuKeyEquivalentsForWindow:(NSWindow*)window
 
     - (void) transformProcessToForeground:(BOOL)activateIfTransformed
     {
+        /* WINE_APP_NAME: override the Dock tooltip + menu-bar app label.
+           macOS caches these in LaunchServices from the binary's bundle at launch
+           time, so NSBundle swizzling + NSMenu titles aren't enough — we have to
+           poke LaunchServices directly via private API _LSSetApplicationInformationItem. */
+        {
+            const char* _n = getenv("WINE_APP_NAME");
+            if (_n && *_n) {
+                static dispatch_once_t _o;
+                dispatch_once(&_o, ^{
+                    NSString* name = [NSString stringWithUTF8String:_n];
+                    [[NSProcessInfo processInfo] setProcessName:name];
+                    /* Swizzle NSBundle so Wine's own menu builder reads the new name. */
+                    Class cls = [NSBundle class];
+                    SEL sel = @selector(objectForInfoDictionaryKey:);
+                    Method m = class_getInstanceMethod(cls, sel);
+                    IMP origImp = method_getImplementation(m);
+                    IMP newImp = imp_implementationWithBlock(^id(NSBundle* self, NSString* key) {
+                        if (self == [NSBundle mainBundle] &&
+                            ([key isEqualToString:(NSString*)kCFBundleNameKey] ||
+                             [key isEqualToString:@"CFBundleDisplayName"])) {
+                            return name;
+                        }
+                        return ((id(*)(id, SEL, NSString*))origImp)(self, sel, key);
+                    });
+                    method_setImplementation(m, newImp);
+                    /* Update LaunchServices for Dock tooltip + menu bar label. */
+                    extern CFStringRef _kLSDisplayNameKey __attribute__((weak));
+                    extern CFStringRef _kLSBundleNameKey __attribute__((weak));
+                    extern OSStatus _LSSetApplicationInformationItem(int, void*, CFStringRef, CFTypeRef, CFDictionaryRef*) __attribute__((weak));
+                    extern void* _LSGetCurrentApplicationASN(void) __attribute__((weak));
+                    if (_LSSetApplicationInformationItem && _LSGetCurrentApplicationASN) {
+                        void* asn = _LSGetCurrentApplicationASN();
+                        if (&_kLSDisplayNameKey)
+                            _LSSetApplicationInformationItem(-2, asn, _kLSDisplayNameKey,
+                                (__bridge CFStringRef)name, NULL);
+                        if (&_kLSBundleNameKey)
+                            _LSSetApplicationInformationItem(-2, asn, _kLSBundleNameKey,
+                                (__bridge CFStringRef)name, NULL);
+                        /* Also try the string form directly in case the weak symbols don't resolve. */
+                        _LSSetApplicationInformationItem(-2, asn,
+                            CFSTR("LSDisplayName"), (__bridge CFStringRef)name, NULL);
+                        _LSSetApplicationInformationItem(-2, asn,
+                            CFSTR("CFBundleName"), (__bridge CFStringRef)name, NULL);
+                    }
+                    /* The Dock tooltip reads NSRunningApplication.localizedName.
+                       For unbundled processes that falls back to the exe basename
+                       ("wine-preloader"). Swizzle it so it returns our custom name. */
+                    {
+                        Class rc = [NSRunningApplication class];
+                        SEL ln = @selector(localizedName);
+                        Method mm = class_getInstanceMethod(rc, ln);
+                        IMP origLn = method_getImplementation(mm);
+                        IMP newLn = imp_implementationWithBlock(^NSString*(NSRunningApplication* self) {
+                            if ([self processIdentifier] == getpid()) return name;
+                            return ((NSString*(*)(id, SEL))origLn)(self, ln);
+                        });
+                        method_setImplementation(mm, newLn);
+                    }
+                    NSRunningApplication* curApp = [NSRunningApplication currentApplication];
+                    [curApp willChangeValueForKey:@"localizedName"];
+                    [curApp didChangeValueForKey:@"localizedName"];
+                });
+            }
+        }
         if ([NSApp activationPolicy] != NSApplicationActivationPolicyRegular)
         {
             NSMenu* mainMenu;
@@ -340,8 +405,20 @@ - (void) transformProcessToForeground:(BOOL)activateIfTransformed
             mainMenu = [[[NSMenu alloc] init] autorelease];
 
             // Application menu
-            submenu = [[[NSMenu alloc] initWithTitle:WineLocalizedString(STRING_MENU_WINE)] autorelease];
-            bundleName = [[NSBundle mainBundle] objectForInfoDictionaryKey:(NSString*)kCFBundleNameKey];
+            /* Honor WINE_APP_NAME env var: lets our launcher override the bundle name
+               so the menu bar app label + Hide/Quit items display e.g. "Zoo Tycoon 2"
+               instead of "CrossOver FOSS 23.7.1". */
+            const char* _wineAppName = getenv("WINE_APP_NAME");
+            NSString* _customAppName = (_wineAppName && *_wineAppName)
+                ? [NSString stringWithUTF8String:_wineAppName] : nil;
+            NSString* _appMenuTitle = _customAppName ? _customAppName : WineLocalizedString(STRING_MENU_WINE);
+            submenu = [[[NSMenu alloc] initWithTitle:_appMenuTitle] autorelease];
+            if (_customAppName) {
+                bundleName = _customAppName;
+                [[NSProcessInfo processInfo] setProcessName:_customAppName];
+            } else {
+                bundleName = [[NSBundle mainBundle] objectForInfoDictionaryKey:(NSString*)kCFBundleNameKey];
+            }
 
             if ([bundleName length])
                 title = [NSString stringWithFormat:WineLocalizedString(STRING_MENU_ITEM_HIDE_APPNAME), bundleName];
@@ -367,7 +444,7 @@ - (void) transformProcessToForeground:(BOOL)activateIfTransformed
             item = [submenu addItemWithTitle:title action:@selector(terminate:) keyEquivalent:@"q"];
             [item setKeyEquivalentModifierMask:NSEventModifierFlagCommand | NSEventModifierFlagOption];
             item = [[[NSMenuItem alloc] init] autorelease];
-            [item setTitle:WineLocalizedString(STRING_MENU_WINE)];
+            [item setTitle:_appMenuTitle];
             [item setSubmenu:submenu];
             [mainMenu addItem:item];
 
@@ -1552,6 +1629,38 @@ - (void) handleMouseMove:(NSEvent*)anEvent
                     point.y -= rect.origin.y;
                 }
 
+                /* Scale cursor for Zoo Tycoon 2 window scaling.
+                   Map from actual macOS window coords to Wine's 640x480 window coords.
+                   Use Wine's fractional frame for the output position since that's
+                   what Wine's coordinate system expects. */
+                if (macdrv_scaling_active && targetWindow &&
+                    [targetWindow isKindOfClass:[WineWindow class]] &&
+                    0 /* disabled - mouse.c handles scaling */) {
+                    NSRect macFrame = targetWindow.frame;
+                    NSRect wineFrame = [(WineWindow*)targetWindow wine_fractionalFrame];
+                    CGFloat screenH = [self primaryScreenHeight];
+                    /* Actual macOS content area in CG coords (top-left origin) */
+                    CGFloat macLeft = macFrame.origin.x;
+                    CGFloat macTop = screenH - macFrame.origin.y - macFrame.size.height;
+                    NSRect macContent = [targetWindow contentRectForFrameRect:macFrame];
+                    CGFloat macTitleH = macFrame.size.height - macContent.size.height;
+                    CGFloat macContentTop = macTop + macTitleH;
+                    /* Wine content area in CG coords */
+                    CGFloat wineLeft = wineFrame.origin.x;
+                    CGFloat wineTop = screenH - wineFrame.origin.y - wineFrame.size.height;
+                    NSRect wineContent = [targetWindow contentRectForFrameRect:wineFrame];
+                    CGFloat wineTitleH = wineFrame.size.height - wineContent.size.height;
+                    CGFloat wineContentTop = wineTop + wineTitleH;
+                    /* Map: macOS content-relative -> game coords -> Wine content-relative */
+                    CGFloat gameX = (point.x - macLeft - g_scaleOffsetX) / g_scaleFactor;
+                    CGFloat gameY = (point.y - macContentTop - g_scaleOffsetY) / g_scaleFactor;
+                    /* Use Wine frame top (not content top) for output,
+                       because Win32 input uses screen coords and Wine's
+                       frame-to-client mapping handles the title bar. */
+                    point.x = wineLeft + gameX;
+                    point.y = wineTop + gameY;
+                }
+
                 point = cgpoint_win_from_mac(point);
 
                 event = macdrv_create_event(MOUSE_MOVED_ABSOLUTE, targetWindow);
@@ -1706,6 +1815,27 @@ - (void) handleMouseButton:(NSEvent*)theEvent
                     pt.y -= rect.origin.y;
                 }
 
+                /* Scale cursor for Zoo Tycoon 2 window scaling */
+                if (macdrv_scaling_active && window &&
+                    [window isKindOfClass:[WineWindow class]] &&
+                    0 /* disabled - mouse.c handles scaling */) {
+                    NSRect macFrame = window.frame;
+                    NSRect wineFrame = [(WineWindow*)window wine_fractionalFrame];
+                    CGFloat screenH = [self primaryScreenHeight];
+                    CGFloat macLeft = macFrame.origin.x;
+                    CGFloat macTop = screenH - macFrame.origin.y - macFrame.size.height;
+                    NSRect macContent = [window contentRectForFrameRect:macFrame];
+                    CGFloat macContentTop = macTop + (macFrame.size.height - macContent.size.height);
+                    CGFloat wineLeft = wineFrame.origin.x;
+                    CGFloat wineTop = screenH - wineFrame.origin.y - wineFrame.size.height;
+                    NSRect wineContent = [window contentRectForFrameRect:wineFrame];
+                    CGFloat wineContentTop = wineTop + (wineFrame.size.height - wineContent.size.height);
+                    CGFloat gameX = (pt.x - macLeft - g_scaleOffsetX) / g_scaleFactor;
+                    CGFloat gameY = (pt.y - macContentTop - g_scaleOffsetY) / g_scaleFactor;
+                    pt.x = wineLeft + gameX;
+                    pt.y = wineTop + gameY;
+                }
+
                 pt = cgpoint_win_from_mac(pt);
 
                 event = macdrv_create_event(MOUSE_BUTTON, window);
@@ -2758,7 +2888,44 @@ int macdrv_get_cursor_position(CGPoint *pos)
     OnMainThread(^{
         NSPoint location = [NSEvent mouseLocation];
         location = [[WineApplicationController sharedController] flippedMouseLocation:location];
-        *pos = cgpoint_win_from_mac(NSPointToCGPoint(location));
+        CGPoint cgpos = NSPointToCGPoint(location);
+
+        /* Scale for Zoo Tycoon 2 window scaling (cursor position polling).
+           Must match the formula in mouse.c send_mouse_input exactly.
+           Scale AFTER cgpoint_win_from_mac so we're in Win32 space. */
+        *pos = cgpoint_win_from_mac(cgpos);
+        if (macdrv_scaling_active && g_actualContentWidth > 650 && g_actualContentHeight > 490 &&
+            g_scaleFactor > 0.01f && g_real_window_right > g_real_window_left) {
+            int rs = retina_on ? 2 : 1;
+            int title_offset = g_fullscreen_active ? 0
+                : (g_real_window_bottom - g_real_window_top - g_actualContentHeight);
+            int relX, relY, gx, gy;
+            if (title_offset < 0) title_offset = 0;
+            /* Scale ref: current on-screen window position. */
+            relX = (int)pos->x - g_real_window_left * rs - (int)(g_scaleOffsetX * rs);
+            relY = (int)pos->y - (g_real_window_top + title_offset) * rs - (int)(g_scaleOffsetY * rs);
+            gx = (int)(relX / g_scaleFactor);
+            gy = (int)(relY / g_scaleFactor);
+            /* Output base: Wine's internal client origin so game's ScreenToClient
+               (which uses Wine's possibly-stale client_rect) yields (gx, gy).
+               Compute from wineFrame which reflects Wine's internal window state. */
+            for (NSWindow* win in [NSApp windows]) {
+                if ([win isKindOfClass:[WineWindow class]] &&
+                    [[win title] containsString:@"Zoo Tycoon 2"] &&
+                    [[win title] length] < 20) {
+                    NSRect wineFrame = [(WineWindow*)win wine_fractionalFrame];
+                    WineApplicationController* ctrl = [WineApplicationController sharedController];
+                    CGFloat screenH = [ctrl primaryScreenHeight];
+                    int wineLeft = (int)(wineFrame.origin.x * rs);
+                    int wineTop = (int)((screenH - wineFrame.origin.y - wineFrame.size.height) * rs);
+                    int wineTitleH = (int)(wineFrame.size.height * rs) - 480 * rs;
+                    if (wineTitleH < 0) wineTitleH = 0;
+                    pos->x = wineLeft + gx;
+                    pos->y = wineTop + wineTitleH + gy;
+                    break;
+                }
+            }
+        }
     });
 
     return TRUE;
diff --git a/dlls/winemac.drv/cocoa_opengl.m b/dlls/winemac.drv/cocoa_opengl.m
index ce8788b..9daf709 100644
--- a/dlls/winemac.drv/cocoa_opengl.m
+++ b/dlls/winemac.drv/cocoa_opengl.m
@@ -21,10 +21,12 @@
 #define GL_SILENCE_DEPRECATION
 #include <OpenGL/gl.h>
 #import "cocoa_opengl.h"
+#import "cocoa_window.h"
 
 #include "macdrv_cocoa.h"
 #include "cocoa_app.h"
 #include "cocoa_event.h"
+#import <QuartzCore/QuartzCore.h>
 
 
 @interface WineOpenGLContext ()
@@ -366,6 +368,7 @@ void macdrv_update_opengl_context(macdrv_opengl_context c)
             });
             [context resetSurfaceIfBackingSizeChanged];
         }
+
     }
 
     [pool release];
@@ -385,5 +388,21 @@ void macdrv_flush_opengl_context(macdrv_opengl_context c)
     macdrv_update_opengl_context(c);
     [context flushBuffer];
 
+    /* Zoo Tycoon 2: pin view to 640x480 after flush to keep GL framebuffer
+       at game resolution. The view gets auto-resized by the window, so we
+       reset it here on every frame. The layer transform in windowDidResize
+       handles the visual scaling. */
+    if (macdrv_scaling_active) {
+        NSView* view = [context view];
+        if (view && (fabs(view.frame.size.width - 640) > 2 || fabs(view.frame.size.height - 480) > 2)) {
+            NSWindow* win = [view window];
+            if (win && [[win title] containsString:@"Zoo Tycoon 2"] && [[win title] length] < 20) {
+                dispatch_async(dispatch_get_main_queue(), ^{
+                    [view setFrameSize:NSMakeSize(640, 480)];
+                });
+            }
+        }
+    }
+
     [pool release];
 }
diff --git a/dlls/winemac.drv/cocoa_window.h b/dlls/winemac.drv/cocoa_window.h
index a83f2aa..ff98212 100644
--- a/dlls/winemac.drv/cocoa_window.h
+++ b/dlls/winemac.drv/cocoa_window.h
@@ -111,3 +111,17 @@ - (void) updateForCursorClipping;
     - (void) setRetinaMode:(int)mode;
 
 @end
+
+extern BOOL macdrv_scaling_active;
+extern NSSize g_lastLargeSize;
+extern BOOL g_forceExpand;
+extern int g_actualContentWidth;
+extern int g_actualContentHeight;
+extern float g_scaleFactor;
+extern float g_scaleOffsetX;
+extern float g_scaleOffsetY;
+extern int g_fullscreen_active;
+extern int g_real_window_left;
+extern int g_real_window_top;
+extern int g_real_window_right;
+extern int g_real_window_bottom;
diff --git a/dlls/winemac.drv/cocoa_window.m b/dlls/winemac.drv/cocoa_window.m
index ec2a292..a9df8a5 100644
--- a/dlls/winemac.drv/cocoa_window.m
+++ b/dlls/winemac.drv/cocoa_window.m
@@ -480,6 +480,23 @@ - (NSFocusRingType) focusRingType
 @end
 
 
+BOOL macdrv_scaling_active = TRUE;
+NSSize g_lastLargeSize = {0, 0};
+BOOL g_forceExpand = FALSE;
+int g_actualContentWidth = 640;
+int g_actualContentHeight = 480;
+float g_scaleFactor = 1.0f;
+float g_scaleOffsetX = 0.0f;
+float g_scaleOffsetY = 0.0f;
+int g_fullscreen_active = 0;
+/* Real on-screen window rect in CG points (Win32 logical coords when rs=1).
+   Used by mouse.c/cocoa_app.m in fullscreen where Wine's NtUserGetWindowRect
+   returns the stale pre-fullscreen rect. */
+int g_real_window_left = 0;
+int g_real_window_top = 0;
+int g_real_window_right = 0;
+int g_real_window_bottom = 0;
+
 @implementation WineContentView
 
 @synthesize everHadGLContext = _everHadGLContext;
@@ -496,6 +513,28 @@ - (instancetype) initWithFrame:(NSRect)frame
         return self;
     }
 
+    - (void) updateTrackingAreas
+    {
+        [super updateTrackingAreas];
+        /* When the view is pinned to 640x480 but the window is larger,
+           expand tracking areas to cover the full window content area
+           so mouse events aren't lost outside the view bounds. */
+        if (macdrv_scaling_active && g_actualContentWidth > 650) {
+            for (NSTrackingArea* area in [self trackingAreas]) {
+                [self removeTrackingArea:area];
+            }
+            NSRect fullRect = NSMakeRect(0, 0, g_actualContentWidth, g_actualContentHeight);
+            NSTrackingArea* expanded = [[NSTrackingArea alloc] initWithRect:fullRect
+                                                                   options:(NSTrackingMouseMoved |
+                                                                            NSTrackingActiveAlways |
+                                                                            NSTrackingMouseEnteredAndExited)
+                                                                     owner:[self window]
+                                                                  userInfo:nil];
+            [self addTrackingArea:expanded];
+            [expanded release];
+        }
+    }
+
     - (void) dealloc
     {
         [markedText release];
@@ -542,6 +581,8 @@ - (void) updateLayer
 
         if (image)
         {
+            if (macdrv_scaling_active)
+                layer.contentsGravity = kCAGravityResize;
             layer.contents = (id)image;
             CFRelease(image);
             [window windowDidDrawContent];
@@ -1044,7 +1085,7 @@ + (WineWindow*) createWindowWithFeatures:(const struct macdrv_window_features*)w
         window.queue = queue;
         window->savedContentMinSize = NSZeroSize;
         window->savedContentMaxSize = NSMakeSize(FLT_MAX, FLT_MAX);
-        window->resizable = wf->resizable;
+        window->resizable = YES;
         window->_lastDisplayTime = [[NSDate distantPast] timeIntervalSinceReferenceDate];
 
         [window registerForDraggedTypes:[NSArray arrayWithObjects:(NSString*)kUTTypeData,
@@ -1108,7 +1149,7 @@ - (void) dealloc
     - (BOOL) preventResizing
     {
         BOOL preventForClipping = cursor_clipping_locks_windows && [[WineApplicationController sharedController] clippingCursor];
-        return ([self styleMask] & NSWindowStyleMaskResizable) && (disabled || !resizable || preventForClipping);
+        return NO;
     }
 
     - (BOOL) allowsMovingWithMaximized:(BOOL)inMaximized
@@ -1124,6 +1165,10 @@ - (BOOL) allowsMovingWithMaximized:(BOOL)inMaximized
     - (void) adjustFeaturesForState
     {
         NSUInteger style = [self styleMask];
+        if (!(style & NSWindowStyleMaskResizable)) {
+            [self setStyleMask:style | NSWindowStyleMaskResizable];
+            style = [self styleMask];
+        }
 
         if (style & NSWindowStyleMaskClosable)
             [[self standardWindowButton:NSWindowCloseButton] setEnabled:!self.disabled];
@@ -1144,6 +1189,8 @@ - (void) adjustFeaturesForState
         {
             [self setContentMaxSize:savedContentMaxSize];
             [self setContentMinSize:savedContentMinSize];
+            /* Don't set aspect ratio here — it constrains programmatic setFrame too.
+               We enforce 4:3 only during user live resize via windowWillResize. */
         }
 
         if (allow_immovable_windows || cursor_clipping_locks_windows)
@@ -1224,7 +1271,7 @@ - (void) setWindowFeatures:(const struct macdrv_window_features*)wf
                 [self setTitle:title];
         }
 
-        resizable = wf->resizable;
+        resizable = YES;
         [self adjustFeaturesForState];
         [self setHasShadow:wf->shadow];
     }
@@ -2167,7 +2214,13 @@ - (void) setWineMinSize:(NSSize)minSize maxSize:(NSSize)maxSize
         if (![self preventResizing])
         {
             [self setContentMinSize:minSize];
-            [self setContentMaxSize:maxSize];
+            /* Override max size for Zoo Tycoon 2 to allow resizing */
+            if ([[self title] containsString:@"Zoo Tycoon 2"] && [[self title] length] < 20) {
+                [self setContentMaxSize:NSMakeSize(FLT_MAX, FLT_MAX)];
+                savedContentMaxSize = NSMakeSize(FLT_MAX, FLT_MAX);
+            } else {
+                [self setContentMaxSize:maxSize];
+            }
         }
     }
 
@@ -2351,7 +2404,7 @@ - (void) grabDockIconSnapshotFromWindow:(WineWindow*)window force:(BOOL)force
 
         const void* windowID = (const void*)(CGWindowID)window.windowNumber;
         CFArrayRef windowIDs = CFArrayCreate(NULL, &windowID, 1, NULL);
-        CGImageRef windowImage = CGWindowListCreateImageFromArray(CGRectNull, windowIDs, kCGWindowImageBoundsIgnoreFraming);
+        CGImageRef windowImage = NULL; /* obsoleted in macOS 15 */
         CFRelease(windowIDs);
         if (!windowImage)
             return;
@@ -2738,8 +2791,15 @@ - (void) displayIfNeeded
 
     - (void) setFrame:(NSRect)frameRect display:(BOOL)flag
     {
-        if (flag)
-            [self setAutodisplay:YES];
+        /* Block Wine's programmatic shrink for Zoo Tycoon 2.
+           Allow user live resize through. */
+        if (macdrv_scaling_active && ![self inLiveResize] &&
+            [[self title] containsString:@"Zoo Tycoon 2"] && [[self title] length] < 20 &&
+            self.frame.size.width > 650 &&
+            frameRect.size.width < self.frame.size.width - 5) {
+            frameRect = self.frame;
+        }
+        if (flag) [self setAutodisplay:YES];
         [super setFrame:frameRect display:flag];
     }
 
@@ -3014,6 +3074,9 @@ - (void) windowDidEnterFullScreen:(NSNotification*)notification
         enteredFullScreenTime = [[NSProcessInfo processInfo] systemUptime];
         if (pendingOrderOut)
             [self doOrderOut];
+        /* Re-apply view pin + layer transform after AppKit finishes fullscreen layout */
+        if (macdrv_scaling_active)
+            [self windowDidResize:nil];
     }
 
     - (void) windowDidExitFullScreen:(NSNotification*)notification
@@ -3074,6 +3137,54 @@ - (void)windowDidResignKey:(NSNotification *)notification
 
     - (void)windowDidResize:(NSNotification *)notification skipSizeMove:(BOOL)skipSizeMove
     {
+        if (macdrv_scaling_active) {
+            roundedWineFrame = self.frame;
+            /* Pin view to 640x480 and scale via layer transform */
+            if ([[self title] containsString:@"Zoo Tycoon 2"] && [[self title] length] < 20) {
+                WineContentView* cv = (WineContentView*)[self contentView];
+                if (cv) {
+                    NSRect content = [self contentRectForFrameRect:self.frame];
+                    /* Track actual content size for mouse coordinate scaling */
+                    g_actualContentWidth = (int)content.size.width;
+                    g_actualContentHeight = (int)content.size.height;
+                    /* Always reset view to 640x480 */
+                    if (fabs(cv.frame.size.width - 640) > 2 || fabs(cv.frame.size.height - 480) > 2) {
+                        [cv setFrameSize:NSMakeSize(640, 480)];
+                    }
+                    /* Scale the 640x480 view to fill the window, maintaining aspect ratio */
+                    CALayer* layer = [cv layer];
+                    g_fullscreen_active = ([self styleMask] & NSWindowStyleMaskFullScreen) ? 1 : 0;
+                    /* Publish real on-screen window rect in Win32/CG top-down coords */
+                    {
+                        NSArray* screens = [NSScreen screens];
+                        CGFloat screenH = [screens count] > 0 ? NSHeight([[screens objectAtIndex:0] frame]) : 0;
+                        NSRect f = self.frame;
+                        g_real_window_left = (int)f.origin.x;
+                        g_real_window_top = (int)(screenH - f.origin.y - f.size.height);
+                        g_real_window_right = g_real_window_left + (int)f.size.width;
+                        g_real_window_bottom = g_real_window_top + (int)f.size.height;
+                    }
+                    if (layer && content.size.width > 10) {
+                        CGFloat sx = content.size.width / 640.0;
+                        CGFloat sy = content.size.height / 480.0;
+                        CGFloat scale = (sx < sy) ? sx : sy;
+                        CGFloat offsetX = (content.size.width - 640.0 * scale) / 2.0;
+                        CGFloat offsetY = (content.size.height - 480.0 * scale) / 2.0;
+                        g_scaleFactor = (float)scale;
+                        g_scaleOffsetX = (float)offsetX;
+                        { FILE*f=fopen("/tmp/scale_debug.txt","w");if(f){fprintf(f,"frame=%.0fx%.0f content=%.0fx%.0f sx=%.3f sy=%.3f scale=%.3f offX=%.0f offY=%.0f fs=%d\n",self.frame.size.width,self.frame.size.height,content.size.width,content.size.height,sx,sy,scale,offsetX,offsetY,([self styleMask]&NSWindowStyleMaskFullScreen)?1:0);fclose(f);} }
+                        g_scaleOffsetY = (float)offsetY;
+                        CATransform3D t = CATransform3DIdentity;
+                        t = CATransform3DTranslate(t, offsetX, offsetY, 0);
+                        t = CATransform3DScale(t, scale, scale, 1.0);
+                        layer.transform = t;
+                        layer.anchorPoint = CGPointMake(0, 0);
+                        layer.position = CGPointMake(0, 0);
+                        layer.masksToBounds = NO;
+                    }
+                }
+            }
+        }
         NSRect frame = self.wine_fractionalFrame;
 
         if ([self inLiveResize])
@@ -3174,6 +3285,17 @@ - (void)windowWillMiniaturize:(NSNotification *)notification
 
     - (NSSize) windowWillResize:(NSWindow*)sender toSize:(NSSize)frameSize
     {
+        if (macdrv_scaling_active && [[self title] containsString:@"Zoo Tycoon 2"] && [[self title] length] < 20) {
+            /* Enforce 4:3 aspect ratio and minimum size during user resize */
+            NSRect contentRect = [self contentRectForFrameRect:NSMakeRect(0, 0, frameSize.width, frameSize.height)];
+            if (contentRect.size.width < 640) contentRect.size.width = 640;
+            if (contentRect.size.height < 480) contentRect.size.height = 480;
+            CGFloat targetW = contentRect.size.height * 4.0 / 3.0;
+            contentRect.size.width = targetW;
+            NSRect adjusted = [self frameRectForContentRect:contentRect];
+            frameSize = adjusted.size;
+            return frameSize;
+        }
         if ([self inLiveResize])
         {
             if (maximized)
diff --git a/dlls/winemac.drv/mouse.c b/dlls/winemac.drv/mouse.c
index 724503f..288a742 100644
--- a/dlls/winemac.drv/mouse.c
+++ b/dlls/winemac.drv/mouse.c
@@ -32,6 +32,17 @@
 
 WINE_DEFAULT_DEBUG_CHANNEL(cursor);
 
+extern int g_actualContentWidth;
+extern int g_actualContentHeight;
+extern float g_scaleFactor;
+extern float g_scaleOffsetX;
+extern float g_scaleOffsetY;
+extern int g_fullscreen_active;
+extern int g_real_window_left;
+extern int g_real_window_top;
+extern int g_real_window_right;
+extern int g_real_window_bottom;
+
 
 static pthread_mutex_t cursor_cache_mutex = PTHREAD_MUTEX_INITIALIZER;
 static CFMutableDictionaryRef cursor_cache;
@@ -134,6 +145,43 @@ static void send_mouse_input(HWND hwnd, macdrv_window cocoa_window, UINT flags,
 
     top_level_hwnd = NtUserGetAncestor(hwnd, GA_ROOT);
 
+    /* Scale mouse coordinates for Zoo Tycoon 2 window scaling.
+       x,y are in Win32 screen space (CG points * 2 on Retina).
+       g_actualContentWidth/Height are CONTENT size in CG points.
+       NtUserGetWindowRect returns the Wine WINDOW rect in Win32 space.
+       Scale content area only (title bar pixels are not scaled). */
+    if ((flags & MOUSEEVENTF_ABSOLUTE) && top_level_hwnd &&
+        g_actualContentWidth > 650 && g_actualContentHeight > 490 &&
+        g_scaleFactor > 0.01f &&
+        g_real_window_right > g_real_window_left)
+    {
+        /* Scale ref: current on-screen window position from cocoa_window.m
+           (Wine's wineFrame goes stale when user drags the window or enters
+           macOS fullscreen). Output base: Wine's actual client area via
+           NtUserClientToScreen so ScreenToClient yields the right 640x480
+           client coord regardless of Wine's internal state. */
+        POINT origin = { 0, 0 };
+        int rs = retina_on ? 2 : 1;
+        int relX, relY, gx, gy;
+        int title_offset;
+        NtUserClientToScreen(top_level_hwnd, &origin);
+        /* In windowed mode, the visible game area starts below the native
+           macOS title bar. g_real_window_top is the window's top; the content
+           area is at g_real_window_top + (window_height - content_height).
+           In fullscreen there's no title. We approximate via wineFrame's
+           non-client height — which is wineFrame.h - 480 in windowed (Wine
+           added ~55 for title/border around the pinned view). */
+        title_offset = g_fullscreen_active ? 0
+            : (g_real_window_bottom - g_real_window_top - g_actualContentHeight);
+        if (title_offset < 0) title_offset = 0;
+        relX = x - g_real_window_left * rs - (int)(g_scaleOffsetX * rs);
+        relY = y - (g_real_window_top + title_offset) * rs - (int)(g_scaleOffsetY * rs);
+        gx = (int)(relX / g_scaleFactor);
+        gy = (int)(relY / g_scaleFactor);
+        x = origin.x + gx;
+        y = origin.y + gy;
+    }
+
     if ((flags & MOUSEEVENTF_MOVE) && (flags & MOUSEEVENTF_ABSOLUTE) && !drag &&
         cocoa_window != macdrv_thread_data()->capture_window)
     {
@@ -873,12 +921,39 @@ done:
  */
 BOOL macdrv_SetCursorPos(INT x, INT y)
 {
-    BOOL ret = macdrv_set_cursor_position(CGPointMake(x, y));
-    if (ret)
-        TRACE("warped to %d,%d\n", x, y);
-    else
-        ERR("failed to warp to %d,%d\n", x, y);
-    return ret;
+    int orig_x = x, orig_y = y;
+    /* Inverse of the scaling applied in send_mouse_input/macdrv_get_cursor_position.
+       The input (x, y) is in Wine's (possibly stale) screen coord space — i.e.,
+       relative to Wine's internal client_rect. Decompose using Wine's own
+       window-rect lookup, then re-project onto the real on-screen window. */
+    if (g_actualContentWidth > 650 && g_actualContentHeight > 490 &&
+        g_scaleFactor > 0.01f && g_real_window_right > g_real_window_left)
+    {
+        HWND fg = NtUserGetForegroundWindow();
+        RECT wr;
+        if (fg && NtUserGetWindowRect(fg, &wr)) {
+            int rs = retina_on ? 2 : 1;
+            int wineTitleH = wr.bottom - wr.top - 480 * rs;
+            int title_offset = g_fullscreen_active ? 0
+                : (g_real_window_bottom - g_real_window_top - g_actualContentHeight);
+            int gx, gy;
+            if (wineTitleH < 0) wineTitleH = 0;
+            if (title_offset < 0) title_offset = 0;
+            gx = x - wr.left;
+            gy = y - wr.top - wineTitleH;
+            x = g_real_window_left * rs + (int)(g_scaleOffsetX * rs) + (int)(gx * g_scaleFactor + 0.5f);
+            y = (g_real_window_top + title_offset) * rs + (int)(g_scaleOffsetY * rs) + (int)(gy * g_scaleFactor + 0.5f);
+        }
+    }
+    (void)orig_x; (void)orig_y;
+    {
+        BOOL ret = macdrv_set_cursor_position(CGPointMake(x, y));
+        if (ret)
+            TRACE("warped to %d,%d\n", x, y);
+        else
+            ERR("failed to warp to %d,%d\n", x, y);
+        return ret;
+    }
 }
 
 
